# SmartRoute Python dependencies
# Works on Windows, Linux, and Mac — pip handles platform differences
# Install: pip install -r requirements.txt

streamlit>=1.40.0       # Web UI dashboard
pydantic>=2.8.0         # Schema validation and data models
requests>=2.28.0        # HTTP client for Ollama API
openpyxl>=3.1.0         # Excel file parsing (.xlsx)
pytesseract>=0.3.10     # OCR for image-based inspection docs
Pillow>=10.0.0          # Image loading for OCR
opencv-python>=4.8.0    # Image preprocessing for better OCR
pypdf>=4.0.0            # PDF text extraction
matplotlib>=3.8.0       # Evaluation accuracy charts
